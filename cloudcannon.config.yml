paths:
  static: ''
  uploads: uploads
timezone: Asia/Singapore
markdown:
  engine: commonmark
  options: {}
collections_config:
  pages:
    path: /
    output: true
    url: /[slug].html
editor:
  default_view: visual
  inputs:
    text:
      type: markdown